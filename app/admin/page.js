export const dynamic = 'force-dynamic'

import { supabase } from '../../lib/supabase.js';

export default function AdminPage() {
  return (
    <div>
      <h1>Admin Page</h1>
      <p>This is a placeholder page to ensure the Vercel build succeeds.</p>
    </div>
  );
}
